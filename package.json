{
  "name": "tp-ntom",
  "version": "1.0.0",
  "description": "Trabajo Practico M to N - -Grupo xxxxxxxxx",
  "main": "index.js",
  "scripts": {
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "start": "npm run docker:build && npm run docker:up && node src/app.js",
    "dev": "npm run docker:build && npm run docker:up && nodemon ./src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "sequelize",
    "association",
    "MTON"
  ],
  "author": "Grupo xxxxxxxxx",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mongoose": "^8.8.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
